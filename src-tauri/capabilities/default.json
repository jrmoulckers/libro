{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the main Libro window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
