dist/
node_modules/
coverage/
pnpm-lock.yaml
.impeccable/

# Vendored design tokens, mirrored verbatim from jrmoulckers/studio by the studio
# sync engine. Formatting them is drift: the engine then skips the file forever.
vendor/

# synced from jrmoulckers/.github — canonical source, not authored here.
# These must cover every path in .studio-sync.lock.json that Prettier can parse,
# or a canon sync turns format:check red on branches that touched none of it.
# Re-check against the lock file when the sync adds a path.
#
# agency.toml and .gitattributes have no Prettier parser today, so they are listed
# against a parser arriving later (a TOML plugin, say) rather than against a break
# that exists now. Coverage that depends on a file staying unparseable is luck.
.github/agents/
.github/skills/
.github/prompts/
.github/instructions/
.github/copilot-instructions.md
.gitattributes
agency.toml
AGENTS.md

# Vendored from jrmoulckers/engineering at a pinned ref and pinned by SHA-256 in
# engineering-configs.lock.json. Formatting these would change their bytes and
# break the hash check, turning an upstream-drift signal into a local edit.
config/engineering/
