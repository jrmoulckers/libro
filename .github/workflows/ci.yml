# =============================================================================
# Libro CI
# =============================================================================
#
# This file is PRODUCT-OWNED. Reusable workflows are "native" to the JRM Studio
# sync engine: it resolves and reports them but never writes them into member
# repos (see jrmoulckers/.github sync/lib/assets.mjs). Edit freely here.
#
# Every input below is declared in the corresponding reusable workflow's
# `on.workflow_call.inputs` block — nothing is guessed.
# =============================================================================
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  lint:
    name: Lint
    uses: jrmoulckers/.github/.github/workflows/reusable-ci-lint.yml@974154df3d0200a2508651fc8e67e56d74cd4519 # main
    permissions:
      contents: read
      packages: read
      pull-requests: read
    with:
      node-version: '22'
      package-manager: pnpm
      lint-command: pnpm lint
      format-check-command: pnpm format:check
      semantic-pr-title: true
      registry-url: https://npm.pkg.github.com
      registry-scope: '@jrmoulckers'

  web:
    name: Web CI
    uses: jrmoulckers/.github/.github/workflows/reusable-ci-web.yml@974154df3d0200a2508651fc8e67e56d74cd4519 # main
    permissions:
      contents: read
      packages: read
    with:
      node-version: '22'
      package-manager: pnpm
      typecheck-command: pnpm typecheck
      test-command: pnpm test
      build-command: pnpm build
      artifact-name: libro-web-build
      artifact-path: dist
      artifact-retention-days: 7
      registry-url: https://npm.pkg.github.com
      registry-scope: '@jrmoulckers'

  security:
    name: Security CI
    # reusable-security-ci intentionally takes no registry inputs: it has no install
    # step. It runs checkout + setup-node + `pnpm audit`, and audit resolves advisory
    # data from the default registry rather than the @jrmoulckers-scoped one, so it
    # stays green once scoped dependencies land. Verified against the workflow source
    # at the pinned SHA. Do not add registry-url/packages: read here.
    uses: jrmoulckers/.github/.github/workflows/reusable-security-ci.yml@974154df3d0200a2508651fc8e67e56d74cd4519 # main
    permissions:
      contents: read
    with:
      node-version: '22'
      package-manager: pnpm
      run-package-audit: true
      run-secret-scan: true
      # Enabled since libro became a public repository: the GitHub dependency graph
      # this job requires is on by default for public repos. The job declares its own
      # `contents: read`, which this call site already grants.
      run-dependency-review: true
      dependency-fail-severity: high

  preview:
    name: Preview Artifact
    if: github.event_name == 'pull_request'
    needs: web
    uses: jrmoulckers/.github/.github/workflows/reusable-deploy-preview.yml@974154df3d0200a2508651fc8e67e56d74cd4519 # main
    permissions:
      contents: read
      packages: read
    with:
      node-version: '22'
      package-manager: pnpm
      build-command: pnpm build
      output-dir: dist
      artifact-name: ${{ needs.web.outputs.artifact-name }}
      registry-url: https://npm.pkg.github.com
      registry-scope: '@jrmoulckers'

  perf:
    name: Performance Budget
    needs: web
    uses: jrmoulckers/.github/.github/workflows/reusable-perf-budget.yml@974154df3d0200a2508651fc8e67e56d74cd4519 # main
    permissions:
      contents: read
      packages: read
    with:
      node-version: '22'
      package-manager: pnpm
      build-command: pnpm build
      output-dir: dist
      artifact-name: ${{ needs.web.outputs.artifact-name }}
      bundle-budget-kb: 2048
      registry-url: https://npm.pkg.github.com
      registry-scope: '@jrmoulckers'
      # Lighthouse is skipped while `url` is empty. Set it once a stable preview
      # or production URL exists; lhci-min-performance / lhci-min-accessibility
      # then default to 90 / 95.
      url: ''
      lhci-min-performance: 90
      lhci-min-accessibility: 95
      lighthouse-public-upload: false
